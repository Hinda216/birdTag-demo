{
    "name": "tags-management-lambda",
    "version": "1.0.0",
    "description": "Lambda function for manually adding and removing tags from files",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "@aws-sdk/client-dynamodb": "^3.0.0",
        "@aws-sdk/lib-dynamodb": "^3.0.0"
    },
    "engines": {
        "node": "22.x"
    }
}